# Elink 0.4.16 Windows Preview

本版本补齐显示刷新率与串流帧率的联动。

- 在远控画质设置中手动选择被控端显示刷新率时，串流帧率会立即同步到相同数值。
- 保留 0.4.15 中的 Tab 转发、底部提示移除、主控端鼠标自动切换，以及被控端显示模式退出恢复。

自动化测试通过（123 项）。Windows 双机游戏串流、不同游戏的光标隐藏行为、显示刷新率切换和退出恢复仍需安装后手动验收。当前 Windows 预览包未签名。
