# Elink 0.4.19 Windows Preview

本版本针对远控画面文字毛糙和 60 FPS 不足进行调整。

- 画面缩放改用平滑采样，减少桌面文字和图标边缘的锯齿；仍保留每帧一次的缩放缓存，避免重复绘制。
- 被控端提前准备下一帧，让 DXCam 采集和颜色转换与当前帧的 H.264 编码重叠执行，减少采集、编码串行等待造成的帧率下降。

自动化测试通过（124 项）。真实 Tailscale 链路、不同显示分辨率、硬件编码器和游戏场景仍需安装后手动验收；当前 Windows 预览包未签名。
