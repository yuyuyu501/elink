# Elink 远程控制

基于 Python 的远程桌面控制工具，支持鼠标、键盘操作和实时屏幕传输。

## 功能

- **远程桌面**：实时查看被控端屏幕，60 FPS 流畅显示
- **鼠标控制**：移动、点击、滚轮
- **键盘控制**：支持组合键（Ctrl/Alt/Shift/Win）
- **带宽自适应**：动态调整画质，目标带宽 8 Mbps
- **变化检测**：画面静止时自动停止发送，节省带宽

## 快速开始

### 安装依赖

```bash
pip install -r requirements.txt
```

### 运行

```bash
python main.py
```

### 使用方式

**被控端（被别人控制）**
1. 选择「被别人控制」模式
2. 设置本机端口（默认 5901）
3. 点击「启动」，等待连接

**控制端（控制别人）**
1. 选择「控制别人」模式
2. 填写被控端 IP 地址和端口
3. 点击「启动」开始远程控制

## 协议

| 类型 | 说明 |
|------|------|
| `CMD` | 键盘/鼠标命令，JSON 格式 |
| `SCR` | 屏幕帧，JPEG 格式 |

## 技术要点

- JPEG 质量动态调整（10-85），目标带宽 8 Mbps
- 缩略图 MD5 变化检测（32×24），静止画面零发送
- `TCP_NODELAY` 禁用 Nagle 算法降低延迟
- 控制命令与屏幕流共用同一 TCP 连接

## 依赖

- Python 3.8+
- pyautogui
- Pillow
